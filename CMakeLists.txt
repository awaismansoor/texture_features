PROJECT(Features)

CMAKE_MINIMUM_REQUIRED(VERSION 2.8) 

FIND_PACKAGE(ITK)
IF(ITK_FOUND)
  INCLUDE(${USE_ITK_FILE})
ENDIF(ITK_FOUND)

FIND_PACKAGE( OpenCV REQUIRED )

ADD_EXECUTABLE(Features main.cxx)
TARGET_LINK_LIBRARIES(Features ${ITK_LIBRARIES} ${OpenCV_LIBS})